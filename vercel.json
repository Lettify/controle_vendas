{
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/trpc/:trpc*",
      "destination": "/api/trpc/[trpc]"
    }
  ],
  "functions": {
    "api/trpc/[trpc].ts": {
      "maxDuration": 10,
      "includeFiles": "server/**"
    }
  },
  "regions": ["gru1"]
}