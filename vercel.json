{
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/trpc/:path*",
      "destination": "/api/trpc/[trpc]"
    }
  ],
  "functions": {
    "api/trpc/[trpc].ts": {
      "maxDuration": 10
    }
  },
  "regions": ["gru1"]
}